$('#group_results').html('<%= escape_javascript render(partial: 'select_group') %>')
if ($('#group_results').html() == '') {
  $('#add_group').hide();
  $('#no_group_results').show().highlight();
} else {
  $('#add_group').show();
  $('#no_group_results').hide();
}
