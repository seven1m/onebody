<h1><%= @person.name %></h1>

<table>
  <% if @person.home_phone.to_s.any? %>
    <tr><td><%= t('privacies.home_phone') %>:</td><td><%= format_phone @person.home_phone %></td></tr>
  <% end %>
</table>

<h2><%= t('people.groups') %></h2>
<%= render :partial => 'groups/person_groups' %>
