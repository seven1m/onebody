<% if person.has_photo? %>
  <%= image_tag url_for(:controller => 'people', :action => 'photo', :id => "#{person.id}.tn.jpg"), :alt => person.name, :class => 'icon' %>
<% else %>
  <% if ['male', 'boy', 'man'].include? person.gender.to_s.downcase %>
    <%= image_tag 'man.tn.gif', :alt => person.name, :class => 'icon' %>
  <% elsif ['female', 'girl', 'woman'].include? person.gender.to_s.downcase %>
    <%= image_tag 'woman.tn.gif', :alt => person.name, :class => 'icon' %>
  <% end %>
<% end %>
