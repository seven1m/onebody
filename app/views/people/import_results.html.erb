<h1><%= t('import.import_results') %></h1>

<p><%= t('import.the_import_is_complete') %><% if @errored.any? %> <%= t('import.with_errors') %><% end %>.</p>

<h2><%= t('import.completed_records') %></h2>

<table>
  <tr><th><%= t('import.first') %></th><th><%= t('import.last') %></th><th><%= t('import.status') %></th></tr>
  <% @completed.each do |record| %>
    <tr>
      <td><%=h record[:first_name] %></td>
      <td><%=h record[:last_name] %></td>
      <td><%=h record[:status] %></td>
    </tr>
  <% end %>
</table>

<% if @errored.any? %>
  <h2><%= t('import.errored_records') %></h2>

  <table>
    <tr><th><%= t('import.first') %></th><th><%= t('import.last') %></th><th><%= t('import.status') %></th></tr>
    <% @errored.each do |record| %>
      <tr>
        <td><%=h record[:first_name] %></td>
        <td><%=h record[:last_name] %></td>
        <td><%=h record[:status] %></td>
      </tr>
      <tr>
        <td colspan="3" style="padding-left:20px;font-size:smaller;"><%=h record[:message] %></td>
      </tr>
    <% end %>
  </table>
<% end %>
