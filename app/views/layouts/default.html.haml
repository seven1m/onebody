!!!
%html
  %head
    %meta{charset: "UTF-8"}
    %title
      CHURCH.IO
      - if @title.present?
        == | #{@title}
    %meta{content: "width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no", name: "viewport"}
    = stylesheet_link_tag 'application'
    %link{href: "//fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,600,300italic,400italic,600italic|Abel", rel: "stylesheet"}
    /[if lt IE 9]
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  %body.skin-blue
    / header logo: style can be found in header.less
    %header.header
      = link_to 'CHURCH.IO', root_path, class: 'logo'
      / Header Navbar: style can be found in header.less
      %nav.navbar.navbar-static-top{role: "navigation"}
        / Sidebar toggle button
        %a.navbar-btn.sidebar-toggle{"data-toggle" => "offcanvas", href: "#", role: "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        .navbar-right
          = render 'layouts/navbar_right'
        %h3.hidden-sm.hidden-xs{style: "float:right;font-size: 18px; line-height: 50px; padding: 0 10px; font-family: Abel, Helvetica, Arial, sans-serif; font-weight: normal; height: 50px; display: block; margin: 0; color: #fff;"} Cedar Ridge Christian Church
    .wrapper.row-offcanvas.row-offcanvas-left
      / Left side column. contains the logo and sidebar
      %aside.left-side.sidebar-offcanvas
        / sidebar: style can be found in sidebar.less
        %section.sidebar
          / Sidebar user panel
          .user-panel
            .pull-left.image
              = link_to @logged_in do
                = avatar_tag @logged_in, class: 'img-circle'
            .pull-left.info
              %p
                Hello,
                = @logged_in.first_name
              %a{href: "#"}
                %i.fa.fa-circle.text-success
                Online
          / search form
          %form.sidebar-form{action: '/search', method: 'get'}
            .input-group
              %input.form-control{name: "q", placeholder: "Search...", type: "text"}
              %span.input-group-btn
                %button#search-btn.btn.btn-flat{name: "seach", type: "submit"}
                  %i.fa.fa-search
          / /.search form
          / sidebar menu: : style can be found in sidebar.less
          %ul.sidebar-menu
            %li
              = link_to root_path do
                %i.fa.fa-home
                %span Home
                %small.badge.pull-right.bg-green 12 new
            %li
              = link_to @logged_in do
                %i.fa.fa-user
                %span Profile
            %li
              = link_to groups_path do
                %i.fa.fa-group
                %span Groups
                %small.badge.pull-right.bg-green 2 new
            %li.treeview
              = link_to new_search_path do
                %i.fa.fa-archive
                %span Directory
                %i.fa.fa-angle-left.pull-right
              %ul.treeview-menu
                %li
                  = link_to search_path(business: true) do
                    %i.fa.fa-angle-double-right
                    Business Directory
                %li
                  = link_to new_printable_directory_path do
                    %i.fa.fa-angle-double-right
                    Printed Directory
                %li
                  = link_to new_search_path(advanced: true) do
                    %i.fa.fa-angle-double-right
                    Advanced Search
            %li
              = link_to admin_path do
                %i.fa.fa-cogs
                %span Admin
                %small.badge.pull-right.bg-orange 2 alerts
          = yield(:sidebar)
        / /.sidebar
      / Right side column. Contains the navbar and content of the page
      %aside.right-side
        - if header = yield(:header).presence
          = header
        - else
          %section.content-header
            = breadcrumbs
            %h1= @title
            -#Tim Morgan
            -#%small
              -#%a{href: "#"} Tim &amp; Jennie Morgan
        %section.content
          = yield
    = javascript_include_tag 'application'
