<h2><%= t('publications.new_publication') %></h2>

<%= form_for @publication, :html => {:multipart => true} do |form| %>
  <%= error_messages_for(form) %>
  <p>
    <%= form.label :name, t('publications.name') %>         <%= form.text_field :name %><br/>
    <%= form.label :description, t('publications.description') %> <%= form.text_area :description, :rows => 3 %><br/>
    <%= form.label :file, t('publications.file') %>         <%= form.file_field :file %>
  </p>
  <p>
    <%= radio_button_tag :send_update_to_group_id, 0, true, :class => 'checkbox' %>
    <%= label_tag :send_update_to_group_id_0, t('publications.notify_no_one'), :class => 'inline' %><br/>
    <% @groups.each do |group| %>
      <%= radio_button_tag :send_update_to_group_id, group.id, false, :class => 'checkbox' %>
      <%= label_tag "send_update_to_group_id_#{group.id}", t('publications.notify_the_group', :group => group.name), :class => 'inline' %>
      <br/>
    <% end %>
  </p>
  <p>
    <%= form.submit t('publications.save_publication') %>
  </p>
<% end %>
