%ul.todo-list.ui-sortable
  - tasks.each do |task|
    %li.task{ class: ("done" if task.completed) }
      = form_for [:complete, task.group, task], html: { class: "complete-task-form" } do |form|
        - if @group.present?
          %span.handle
            %i.fa.fa-ellipsis-v
            %i.fa.fa-ellipsis-v  
        = form.check_box :completed, autocomplete: "off"
        %span.text
          = link_to task.name, [task.group, task]
        - if task.duedate
          %span.label.label-warning
            %i.fa.fa-calendar 
            = l(task.duedate)
        - if task.person
          %span.label.label-success
            %i.fa.fa-user 
            = task.person.name
        .tools.show
          =link_to "", edit_group_task_path(task.group, task), class: "fa fa-edit"
          =link_to "", "#", class: "fa fa-trash-o"