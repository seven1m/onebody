<h1><%= I18n.t('privacies.group_privacy') %></h1>

<% if @memberships.any? %>
  <p><%= I18n.t('privacies.you_are_sharing_with_groups', :sharing => h(params[:sharing].humanize)) %></p>
  <ul>
    <% @memberships.each do |membership| %>
      <li><%= link_to membership.group.name,
        edit_group_membership_privacy_path(membership.group, membership) %></li>
    <% end %>
  </ul>
<% else %>
  <p><%= I18n.t('privacies.you_are_not_sharing_with_groups', :sharing => h(params[:sharing].humanize)) %></p>
<% end %>
