<p><%= link_to '&laquo; back to group', group_url(:id => @group, :anchor => 'prayerrequests') %></p>

<h1>Answered Prayers</h1>

<% if @reqs.any? %>
  <% @reqs.each do |req| %>
    <%= render :partial => 'prayer_request', :locals => {:req => req} %>
  <% end %>
<% else %>
  <p><em>none yet</em></p>
<% end %>