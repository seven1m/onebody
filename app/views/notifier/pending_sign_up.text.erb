<%=raw t('notifier.pending_sign_up.intro',
         person: @person.name,
         site: Setting.get(:url, :site)) %>

<%=raw Person.human_attribute_name(:first_name) %>: <%=raw @person.first_name %>
<%=raw Person.human_attribute_name(:last_name) %>: <%=raw @person.last_name %>
<%=raw Person.human_attribute_name(:email) %>: <%=raw @person.email %>

<%=raw t('notifier.pending_sign_up.instruction_first') %>

<%=raw Setting.get(:url, :site) %>people/<%= @person.id %>/edit#tab-visibility

<%=raw t('notifier.pending_sign_up.instruction_last') %>
