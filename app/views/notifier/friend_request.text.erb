<%=raw t('notifier.friend_request.intro',
         person: @person.name,
         site: Setting.get(:name, :site)) %>

<%=raw t('notifier.friend_request.instruction') %>

<%=raw "#{Setting.get(:url, :site)}people/#{@friend.id}/friends" %>
