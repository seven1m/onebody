<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
  <p style="margin-top:15px;padding-top:15px;font-size:12px; font-family:courier new,courier,monospace;">
<%=raw t('notifier.membership_request.intro',
         person: @person.name,
         group: @group.name) %>
  </p>
  <p style="border-top:1px solid #ccc;margin-top:15px;padding-top:15px;font-size:12px; font-family:courier new,courier,monospace;">
<%=raw t('notifier.membership_request.instruction') %>
  </p>
  <p>
<%=raw "#{Setting.get(:url, :site)}groups/#{@group.id}/memberships" %>
  </p>
  </body>
</html>
