<%= @person.name %> has requested to join the group <%= @group.name %>.

To approve or ignore, sign in and visit the following address:
<%= Setting.get(:url, :site) %>groups/membership_requests/<%= @group.id %>