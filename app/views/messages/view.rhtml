<% content_for :sidebar do %>
  <% if @message.group %>
    <p><%= link_to '&laquo; back to group', :controller => 'groups', :action => 'view', :id => @message.group %></p>
  <% elsif person = (@message.to || @message.wall) and @logged_in.sees? person %>
    <p><%= link_to "&laquo; back to #{person.name}", :controller => 'people', :action => 'view', :id => person %></p>
  <% end %>
  <% if @message.person == @logged_in or @logged_in.can_edit? @message %>
    <p>
      <%= link_to 'Delete Message', {:action => 'delete', :id => @message}, :confirm => 'Are you sure?', :method => 'post' %>
    </p>
  <% end %>
<% end %>

<h1><%=h @message.subject %></h1>

<% if @message.wall %>
  <p>
    <strong>Wall:</strong>
    <%= link_to @message.wall.name, :controller => 'people', :action => 'view', :id => @message.wall %>
  </p>
<% end %>

<div class="message-details">
  <div style="width:32px;float:left;margin:4px;">
    <a href="<%= url_for :controller => 'people', :action => 'view', :id => @message.person %>">
      <%= render :partial => 'people/thumbnail', :locals => {:person => @message.person} %>
    </a>
  </div>
  <%= image_tag 'comment.gif', :alt => 'Message', :class => 'no-border', :style => 'vertical-align:middle;' %>
  Message by <%= link_to h(@message.person.name), :controller => 'people', :action => 'view', :id => @message.person %><br/>
  <%= @message.created_at.strftime '%B %d, %Y %I:%M %p' %>
</div>

<% if @message.flagged? and @logged_in.admin? %>
  <p><%= preserve_breaks(remove_excess_breaks(hide_contact_details(@message.flagged_body)), false) %></p>
<% else %>
  <p><%= preserve_breaks(remove_excess_breaks(hide_contact_details(@message.body))) %></p>
<% end %>

<%= render :partial => 'attachments', :locals => {:message => @message} %>

<% @message.children.each do |reply| %>
  <div class="message">
    <div class="message-details">
      <div style="width:32px;float:left;margin:4px;">
        <a href="<%= url_for :controller => 'people', :action => 'view', :id => reply.person %>">
          <%= render :partial => 'people/thumbnail', :locals => {:person => reply.person} %>
        </a>
      </div>
      <%= image_tag 'comment.gif', :alt => 'Reply', :class => 'no-border', :style => 'vertical-align:middle;' %>
      Reply by <%= link_to h(reply.person.name), :controller => 'people', :action => 'view', :id => reply.person %><br/>
      <%= reply.created_at.strftime '%B %d, %Y %I:%M %p' %>
    </div>
    <p>
      <% if params[:show_quoted] %>
        <%= preserve_breaks(remove_excess_breaks(hide_contact_details(reply.body))) %>
      <% else %>
        <% trimmed = remove_bulk_quoting(hide_contact_details(reply.body)) %>
        <%= preserve_breaks(remove_excess_breaks(trimmed)) %>
        <% if reply.body.split(/\n/).length != trimmed.split(/\n/).length %>
          <br/>
          <em style="color:#009;">
            Some quoted text in this message is hidden;
            <%= link_to 'Click here', :show_quoted => true %>
            to show the entire message.
          </em>
        <% end %>
      <% end %>
    </p>
    <%= render :partial => 'attachments', :locals => {:message => reply} %>
  </div>
<% end %>

<% if @message.group and @message.group.can_post? @logged_in %>
  <p>
    <%= link_to_remote 'Reply to this message &raquo;', :url => {:action => 'edit', :parent_id => @message.id} %>
  </p>
<% end %>

<div id="reply"></div>
