<%= form_for Picture.new, :html => {:multipart => true} do |form| %>
  <%= hidden_field_tag :album_id, @album.id %>
  <%= file_field_tag 'pictures[]', :multiple => true %>
  <%= submit_tag t('pictures.upload') %>
<% end %>
