- @title = t('.heading')

- if @logged_in.can_create?(@group.albums.new)
  .page-controls
    = link_to [:new, @group || @person, :album], class: 'btn btn-success' do
      = icon 'fa fa-plus'
      = t('albums.new.button')

= render partial: 'albums'
