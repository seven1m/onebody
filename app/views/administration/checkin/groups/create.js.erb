$("#groups_for_time_<%= @time.id %>").html('<%= escape_javascript render(:partial => 'group', :collection => @groups, :locals => {:time => @time}) %>');
// page.sortable "groups_for_time_#{@time.id}",
//  :url => reorder_administration_checkin_groups_path(:time_id => @time.id)
