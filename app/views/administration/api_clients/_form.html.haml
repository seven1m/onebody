= form_for @client, url: path, method: action do |form|

  = error_messages_for(form)
  .form-group
    = form.label :name, t('admin.api_clients.fields.name')
    = form.text_field :name, class: 'form-control'
  .form-group
    = form.button t('save'), class: 'btn btn-success'