- @title = t('admin.custom_fields.heading')

%p= t('admin.custom_fields.intro')

- @tabs.each do |tab|
  = render partial: 'tab', locals: { tab: tab }

.push-down
  = link_to t('admin.custom_field_tabs.new.button'), new_administration_custom_field_tab_path, class: 'btn btn-success'
