<%= link_to h(message.person.name), person_path(message.person) %>
<% if @person %>(<%= link_to 'wall to wall', with_person_wall_path(@person, :id => message.person) %>)<% end %>
<%= message.created_at.to_s %>
<% if @logged_in.can_edit?(message) %>
  <%= link_to '[delete]', message_path(message), :method => 'delete', :class => 'discreet', :confirm => 'Are you sure?' %>
<% end %>
<br/>
<%= preserve_breaks message.body %>
