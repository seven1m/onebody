<%= link_to_function t('wall.post_a_message'), "Element.show('wall_message'); $('message_body').focus();" %>

<div id="wall_message">
  <%= form_for Message.new(:wall => @person), :remote => true, :html => {:style => 'border:none;'}, :update => 'wall_content' do |form| %>
    <%= form.hidden_field :wall_id %>
    <em><%= t("wall.to_send_a_private_message_html", :url => new_message_path(:to_person_id => @person)) %></em><br/>
    <%= form.text_area :body, :rows => 3, :cols => 40 %>
    <strong><%= t('wall.respect_privacy') %></strong><br/>
    <%= submit_tag t('wall.save_message')%>
  <% end %>
</div>

<script type="text/javascript">Element.hide('wall_message');</script>
