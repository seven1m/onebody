<% controller ||= object.class.name.pluralize.downcase %>
<% if object.tags.any? %>
  <% object.tags.each do |tag| %>
    <%= link_to h(tag.name), tag %>
    <% if params[:controller] == controller %>
      <% if controller == 'verses' %>
        <% url = verse_path(:id => object, :remove_tag => tag.name) %>
      <% elsif controller == 'recipes' %>
        <% url = recipe_path(:id => object, :remove_tag => tag.name) %>
      <% end %>
      <%= link_to 'x', url, :title => I18n.t("delete_this_tag"), :class => 'discreet', :style => 'margin-left:-5px;', :method => 'put', :confirm => I18n.t("are_you_sure_you_want_delete_this_tag")%>
    <% end %>
    <% if defined?(br) and br %><br/><% end %>
  <% end %>
<% else %>
  <em><%=I18n.t("no_tags")%></em>
<% end %>
