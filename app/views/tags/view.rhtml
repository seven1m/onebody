<h1>Tag: <%=h @tag.name %></h1>

<% if @tag.verses.any? %>
  <h2>Verses</h2>
  <% @tag.verses.each do |verse| %>
    <%= render :partial => 'verses/verse', :locals => {:verse => verse} %>
  <% end %>
<% end %>