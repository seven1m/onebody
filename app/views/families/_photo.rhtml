<div>
  <% if @family.has_photo? %>
    <%= link_to image_tag(url_for(:controller => 'families', :action => 'photo', :id => "#{@family.id}.medium.jpg"), :alt => @family.name),
      :controller => 'families', :action => 'photo', :id => "#{@family.id}.jpg", :back => url_for(params) %>
  <% end %>
</div>