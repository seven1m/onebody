<div>
  <% if @family.photo.exists? %>
    <%= link_to image_tag(@family.photo.url(:medium), :alt => @family.name),
      defined?(url) ? url : @family.photo.url(:large) %>
    <% elsif params[:action] == 'edit' %>
      <%= image_tag 'family.jpg', :alt => @family.name %>
    <% end %>
</div>
