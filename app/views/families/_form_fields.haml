.form-group
  = form.label :name, t('families.name')
  = form.text_field :name, onkeyup: 'set_last_name()', class: 'form-control'
  = t('families.name_example_html')
.form-group
  = form.label :last_name, t('families.last_name')
  = form.text_field :last_name, class: 'form-control'
.form-group
  = form.label :home_phone, t('privacies.home_phone')
  = form.phone_field :home_phone, class: 'form-control'
.form-group
  = form.label :address1, t('privacies.address')
  = form.text_field :address1, class: 'form-control'
.form-group
  = form.label :address2, t('privacies.address2')
  = form.text_field :address2, class: 'form-control'
.form-group
  = form.label :city, t('families.city_st_zip')
  %br
  = form.text_field :city, class: 'form-control inline'
  = form.text_field :state, size: 4, class: 'form-control inline'
  = form.text_field :zip, size: 11, class: 'form-control inline'

:javascript
  function set_last_name() {
    var parts = $('#family_name').val().split(' ');
    $('#family_last_name').val(parts.length > 1 ? parts[parts.length-1] : '');
  }
